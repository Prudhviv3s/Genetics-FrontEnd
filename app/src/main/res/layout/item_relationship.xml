<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    android:background="@drawable/relationship_card_background"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="20dp">

    <TextView
        android:id="@+id/tv_relationship_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="32sp" />

    <!-- Optional fallback ImageView if icons are used instead of emojis -->
    <ImageView
        android:id="@+id/iv_relationship_icon"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:visibility="gone" />

    <TextView
        android:id="@+id/tv_relationship_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:textColor="#424242"
        android:textSize="14sp"
        android:textStyle="bold" />

</LinearLayout>
